# Assets

Screenshots of the Stardew Valley Perfection Tracker.

## How they were taken

- Run in demo mode: `npm run dev` in `client/`, then open `http://localhost:5173`.
- Captured with the Windows Snipping Tool (`Win + Shift + S`) and saved as PNG.
- Taken on [ADD: date]. The app has no styling yet, so these will be retaken after styling is added.

## Updating them

Keep the same file names when you replace a screenshot, so the README links keep working. If you add a new screenshot, add a row to the table above and embed it in the main README's Screenshots section.
