/**
 * FarmerLevel (route: "/farmer-level")
 * Uses SkillTracker. Data: skill levels (farming, mining, combat, etc.) + XP.
 */

// FarmerLevel (route: "/farmer-level")
// TODO: uses SkillTracker
export default function FarmerLevel() {
  return <p>Farmer Level - TODO</p>
}