/**
 * Bundles (route: "/bundles")
 * Uses ListTracker. Data: Community Center (or Joja) bundle items + status.
 */

// Bundles (route: "/bundles")
// TODO: wire up like ProduceShipped.jsx once ready - uses ListTracker
export default function Bundles() {
  return <p>Bundles - TODO</p>
}