/**
 * Dashboard (route: "/")
 * Landing page. Shows overall % perfection + one DashboardSummaryCard
 * per tracked category, pulling totals from each category's data source.
 */

// Dashboard (route: "/")
// TODO: replace with real summary cards pulling from getSummary()
export default function Dashboard() {
  return <p>Dashboard - TODO</p>
}