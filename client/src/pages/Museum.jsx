/**
 * Museum (route: "/museum")
 * Uses CardGridTracker (lg size). Data: artifacts + minerals, found/donated status.
 */

// Museum (route: "/museum")
// TODO: uses CardGridTracker (lg size)
export default function Museum() {
  return <p>Museum - TODO</p>
}